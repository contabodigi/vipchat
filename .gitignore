# Xcode / build artifacts (regenerated by Codemagic)
VipChat.xcodeproj/
build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store

# Swift Package Manager
.build/
Package.resolved

# Secrets you add during ship setup — keep out of git; supply via Codemagic if preferred.
# (GoogleService-Info.plist is safe to commit for CI, but leave it to your choice.)
*.p8
*.p12
*.mobileprovision
